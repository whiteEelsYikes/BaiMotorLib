# 白的电机驱动库 py验证版

> `BaiMotorLib`、`MotorSimulation`、`sim.py`是全部实现，其中`sim.py`是启动的py脚本。
>
> 很简单加上py本身也很简洁，不做过多介绍。

一个py实现的pyqt可视化针对`BaiMotorLib`的电机仿真与验证程序。

## 依赖

> 直接运行，报错缺什么补什么。

py3.12或以上

## 运行

直接运行`sim.py`
